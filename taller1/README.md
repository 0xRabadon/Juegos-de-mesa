# Next.js 15.5.4 Portfolio Grid

This project is a minimal Next.js app showing a two-column portfolio grid with images, titles, and descriptions.

## Usage

1. Put your images in `public/images` and rename them `project1.jpg`, `project2.jpg`, etc.
2. Run:

```bash
npm install
npm run dev
```

3. Open http://localhost:3000
